
  {{#each Comment}}

    <div class="comments">
        <h3 class="card-header">{{user.name}}, {{format_date created_at}}</h3>

        <div class="comment card-body">
            <div>
                {{body}}
            </div>
            <div class="meta">
                -{{user.name}}, {{format_date created_at}}
            </div>
        </div>
    </div>
  {{/each}}


